<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Payment Bill Storage Disk
    |--------------------------------------------------------------------------
    |
    | 账单文件存储磁盘配置，用于存储支付平台下载的账单文件和证书文件
    |
    */
    'storage_disk' => 'local',

    /*
    |--------------------------------------------------------------------------
    | HTTP Configuration
    |--------------------------------------------------------------------------
    |
    | 调用支付平台 API 时的 HTTP 客户端配置
    |
    */
    'http' => [
        'timeout' => 60,
        'connect_timeout' => 10,
    ],

    /*
    |--------------------------------------------------------------------------
    | Logger Configuration
    |--------------------------------------------------------------------------
    |
    | 支付平台 SDK 日志配置
    |
    */
    'logger' => [
        'enable' => false,
        'level' => 'info',
        'type' => 'daily',
        'max_file' => 14,
        'channel' => null,
    ],

    /*
    |--------------------------------------------------------------------------
    | Queue Configuration
    |--------------------------------------------------------------------------
    |
    | 账单导入队列配置
    |
    */
    'queues' => [
        'connection' => null, // null 表示使用默认队列连接
        'wechat_bill_import' => 'payment-bill:wechat-import',
        'alipay_bill_import' => 'payment-bill:alipay-import',
    ],

    /*
    |--------------------------------------------------------------------------
    | API 路由前缀
    |--------------------------------------------------------------------------
    |
    | Payment Bill 所有 API 路由的 URL 前缀。
    | 默认值 'api/payment-bill' 会生成如 /api/payment-bill/payment-channels 的路由。
    |
    */
    'route_prefix' => 'api/payment-bill',

    /*
    |--------------------------------------------------------------------------
    | 认证守卫
    |--------------------------------------------------------------------------
    |
    | 指定用于 Payment Bill 认证的守卫名称，默认使用 Sanctum。
    |
    */
    'guard' => 'sanctum',
];
